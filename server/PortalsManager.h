//
// Created by cecix on 15/06/19.
//

#ifndef PORTAL_PORTALSMANAGER_H
#define PORTAL_PORTALSMANAGER_H

#include <unordered_map>
#include "Portal.h"
#include "Stage.h"

class PortalsManager {
/*private:
    std::unordered_map<std::string, Portal*>& portals;
    Stage* stage;
    b2World* world;

public:
    PortalsManager(std::unordered_map<std::string, Portal*>& portals,
    Stage* stage, b2World* world);
    void managePortals(Chell* chell, std::string id);*/
};


#endif //PORTAL_PORTALSMANAGER_H
